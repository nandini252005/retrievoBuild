# Frontend

React + Vite scaffold for the Lost & Found application.

## Setup

```bash
cd frontend
npm install
npm run dev
```

## Environment

Copy `.env.example` to `.env` and adjust as needed:

```bash
cp .env.example .env
```

- `VITE_API_BASE_URL` defaults to `http://localhost:5000/api`
